[all]
exclude-files = [
    "tests/*",
    "src/bindgen/*",
    "example/*",
    "target/*",
    "apple/*",
    ".swiftpm/*",
    "scripts/*",
    ".build/*",
    "Package.swift",
]
verbose = false
all-features = true
timeout = "5m"
skip-clean = true
out = ["Xml"]
