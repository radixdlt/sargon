use crate::prelude::*;

dummy_sargon!(NotarizedTransaction);

// impl NotarizedTransaction {
//     pub fn new
/*
public init(
        signedIntent: SignedIntent,
        notarySignature: SLIP10.Signature
    ) throws {
        sargon()
    }

    public func compile() throws -> Data {
        sargon()
    }
*/
// }
